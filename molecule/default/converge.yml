---
- name: Converge
  hosts: all
  tasks:
    - name: "Include ansible-wireguard"
      include_role:
        name: "ansible-wireguard"
      tags:
        - always
  vars:
    wireguard_ui:
      password: abc123
